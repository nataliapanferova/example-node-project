{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "An example server for concurrent async requests.",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "dev": "source .env && ts-node ./src/server.ts",
    "prod": "npm run build && npm run start"
  },
  "author": "Natalia Panferova",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.4",
    "request": "^2.88.0",
    "request-promise-native": "^1.0.7"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/request-promise-native": "^1.0.15"
  }
}
